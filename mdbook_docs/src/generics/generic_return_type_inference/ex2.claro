var l = [1,2,3];

var unsafeRes: int = Index::get(l, 1);
var safeRes: SafeRes = Index::get(l, 1);

print(unsafeRes);
print(safeRes);

safeRes = Index::get(l, 10);
print(safeRes);

# Out of bounds runtime err.
# unsafeRes: int = Index::get(l, 10);

# Compiler error:
# Ambiguous Contract Procedure Call: Calls to the procedure `Index$<T, R>::get` is ambiguous without an explicit type annotation to constrain the expected generic return type `R`.
# var ambiguous = Index::get(l, 10);
