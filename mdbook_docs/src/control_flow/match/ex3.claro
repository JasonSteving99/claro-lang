$$var x = 2;
$$
$$var sizeStr: string;
$$match (x) {
$$  case 1 ->
$$    sizeStr = "small";
  case 2 ->
    print("More than one line of code is valid.");
    sizeStr = "medium";
$$  case _ ->
$$    sizeStr = "large";
$$}
$$
$$print(sizeStr);
